<Page
    x:Class="ListBox_Control.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ListBox_Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListBox Name="MyListBox" 
                 HorizontalAlignment="Center" 
                 VerticalAlignment="Center"
                     SelectionMode="Multiple" 
                     SelectionChanged="ListBox_SelectionChanged">
            <ListBoxItem Content="Ahmed" />
            <ListBoxItem Content="Mahmoud" />
            <ListBoxItem Content="Hussein" />
            <ListBoxItem Content="Saied" />
            <ListBoxItem Content="Abdo" />
            <ListBoxItem Content="Mohammed" />

        </ListBox>
        <TextBlock Name="ListBoxResult"  VerticalAlignment="Bottom"/>
    </Grid>
</Page>
